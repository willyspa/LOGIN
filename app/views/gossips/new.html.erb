<h1>Créer votre Gossip</h1>
<% if false %>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for @gossip do |f| %>
      <%= f.label :title %>
      <%= f.text_field :title %>
      <br>
      <%= f.label :content %>
      <%= f.text_area :content %>
      <br>
      <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
      <%= f.submit "Create my gossip", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
<% end %>

  <%= form_tag url_for(action: 'create'), method: "post" do %>
    <div class="col-lg-8 offset-lg-2 firm-group">
      <%= label_tag 'Title' %>
      <%= text_field_tag 'title', @gossip.title, class: "form-control", placeholder: "Enter gossip's title" %>
    </div>
    <div class="col-lg-8 offset-lg-2 firm-group">
      <%= label_tag 'Content' %>
      <%= text_area_tag 'content', @gossip.content, class: "form-control" %>
    </div>
    <div class="d-flex justify-content-center">
      <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
      <%= submit_tag "Create Gossip", class: "btn btn-primary"%>
    </div>
  <% end %>


  <% if @gossip.errors.any? %>
    <p>Nous n'avons pas réussi à créer le potin pour la (ou les) raison(s) suivante(s) :</p>
    <ul>
      <% @gossip.errors.full_messages.each do |message| %>
        <li><%= message %></li>
  <% end %>
    </ul>

  <% end %>
